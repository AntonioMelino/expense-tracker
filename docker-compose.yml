services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: expensetracker
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
